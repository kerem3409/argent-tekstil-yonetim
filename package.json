{
  "name": "argent-tekstil-yonetim",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test:reports": "node --experimental-strip-types --test scripts/check-reports.ts",
    "test:finance": "node --experimental-strip-types --test scripts/check-finance.ts",
    "test:sales": "node --experimental-strip-types --test scripts/check-sales.ts",
    "test:production": "node --experimental-strip-types --test scripts/check-production.ts scripts/check-production-workflow.ts scripts/check-production-cards.ts scripts/check-production-print.mjs",
    "test:inventory": "node --experimental-strip-types --test scripts/check-inventory.ts",
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -b",
    "check": "node --experimental-strip-types scripts/check-navigation.ts && npm run test:contacts && npm run test:products && npm run test:inventory && npm run test:production && npm run test:sales && npm run test:finance && npm run test:reports && npm run test:workflow",
    "test:workflow": "node --experimental-strip-types --test scripts/check-workflow.ts",
    "test:products": "node --experimental-strip-types --test scripts/check-products.ts scripts/check-product-definitions.ts",
    "test:contacts": "node --experimental-strip-types --test scripts/check-contacts.ts",
    "test": "node --experimental-strip-types --test scripts/check-*.ts scripts/check-production-print.mjs"
  },
  "dependencies": {
    "react": "^19.3.0",
    "react-dom": "^19.3.0",
    "react-router-dom": "^7.18.4"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "@types/react": "^19.3.0",
    "@types/react-dom": "^19.3.0",
    "@vitejs/plugin-react": "^6.1.1",
    "typescript": "^7.0.2",
    "vite": "^8.3.0"
  }
}
